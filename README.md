# @tscircuit/fixed-via-hypergraph-solver

A dedicated package for fixed via hypergraph solving logic used by the autorouter stack.

This repository is currently in bootstrap phase. Solver implementation will be added in follow-up PRs.

## Build

```bash
bun run build
```
